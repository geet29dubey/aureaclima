import type { Dictionary, Locale } from "@/i18n/dictionaries";
import { Brand, Icon } from "./icon";
import { LanguageSelector, MobileMenu } from "./navigation";
import { ServicesDropdown } from "./services-dropdown";
export function Header({ locale, d }: { locale: Locale; d: Dictionary }) {
  return <><a className="skip-link" href="#main">{d.nav.skip}</a><div className="demo-banner"><div className="container banner-inner"><span><span className="demo-pill">DEMO</span>{d.banner}</span><a href={`/${locale}#contact-rooklyn`}>{d.bannerCta}<Icon name="arrow" size={15}/></a></div></div><header className="site-header"><div className="container header-inner"><a href={`/${locale}`} aria-label="Áurea Clima"><Brand/></a><nav className="desktop-nav" aria-label={d.nav.menu}><a href={`/${locale}`} className="nav-home">{d.nav.home}</a><ServicesDropdown locale={locale} label={d.nav.services}/><a href={`/${locale}#how-it-works`}>{d.nav.how}</a><a href={`/${locale}#faq`}>{d.nav.faq}</a></nav><div className="header-actions"><div className="desktop-language"><LanguageSelector locale={locale} label={d.nav.language}/></div><a className="button button-gold header-cta" href={`/${locale}#services`}>{d.nav.cta}<Icon name="arrow" size={17}/></a><a className="button button-outline header-contact" href={`/${locale}#contact-rooklyn`}>{d.nav.contact}<Icon name="arrow" size={16}/></a><MobileMenu locale={locale} d={d.nav}/></div></div></header></>;
}
